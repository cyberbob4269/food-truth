---
description: Food Truth WhatsApp freemium, Stripe unlock, and required skills
alwaysApply: true
---

# Food Truth monetization

## Product

- Static SPA: `index.html` in this folder (source of truth).
- WhatsApp-shareable HTTPS site (GitHub Pages). Free staples; £3.99 unlocks all.

## Free categories (no paywall)

`dairy-milk`, `bread`, `cheddar`, `eggs`, `butter`

## Stripe (Netwrxsolutions `acct_1OFMQeDHa6mDnQGT`)

| Field | Value |
|-------|--------|
| Product | `prod_Ut6PW2XiksNAUg` — Food Truth — Full Unlock |
| Price | `price_1TtKCRDHa6mDnQGTRr1jbbVX` — £3.99 GBP one-time |
| Payment Link | set in `index.html` as `FOODTRUTH_PAYMENT_LINK` after create |

Post-pay: redirect to hosted app `?unlock=1&session_id={CHECKOUT_SESSION_ID}` → `localStorage` unlock on that device.

Mirror xTalk ops: durable HTTPS URL only (never ephemeral tunnels for live Payment Link redirects). Skill pattern: `xtalk-stripe-sales`.

## Skills to use

- `stripe-best-practices` + Stripe MCP — Payment Links, products, prices (never send `payment_method_types` unless Terminal)
- `xtalk-stripe-sales` — Netwrx Stripe account / durable-redirect lessons
- `github-cursor-auth` — `gh` for Pages / repo
- `speak-chat-answers` — speak every reply aloud

## Out of scope unless asked

Mobile UI redesign, PWA, barcode scan, native apps.
